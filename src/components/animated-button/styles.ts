import {StyleSheet} from 'react-native';
import {UIStyles} from '../../assets/styles';

export default StyleSheet.create({
  login: {
    ...UIStyles.button,
    backgroundColor: 'rgba(217, 57, 49, 0.5)',
  },
  center: {
    alignItems: 'center',
  },
});
